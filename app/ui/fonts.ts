import { Inter, Lusitana, New_Rocker } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const lusitana = Lusitana({
  subsets: ["latin"],
  weight: ["700", "400"],
});
export const newRocker = New_Rocker({ subsets: ["latin"], weight: ["400"] });
